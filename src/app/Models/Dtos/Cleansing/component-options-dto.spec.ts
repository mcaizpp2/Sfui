import { ComponentOptionsDto } from './component-options-dto';

describe('ComponentOptionsDto', () => {
  it('should create an instance', () => {
    expect(new ComponentOptionsDto()).toBeTruthy();
  });
});
