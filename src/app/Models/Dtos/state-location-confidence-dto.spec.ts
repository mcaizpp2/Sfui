import { StateLocationConfidenceDto } from './state-location-confidence-dto';

describe('StateLocationConfidenceDto', () => {
  it('should create an instance', () => {
    expect(new StateLocationConfidenceDto()).toBeTruthy();
  });
});
