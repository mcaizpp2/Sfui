import { DeleteLoadValResponse } from './delete-load-val-response';

describe('DeleteLoadValResponse', () => {
  it('should create an instance', () => {
    expect(new DeleteLoadValResponse()).toBeTruthy();
  });
});
