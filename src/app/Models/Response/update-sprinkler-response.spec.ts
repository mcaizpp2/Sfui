import { UpdateSprinklerResponse } from './update-sprinkler-response';

describe('UpdateSprinklerResponse', () => {
  it('should create an instance', () => {
    expect(new UpdateSprinklerResponse()).toBeTruthy();
  });
});
