import { AuthenticateResponse } from './authenticate-response';

describe('AuthenticateResponse', () => {
  it('should create an instance', () => {
    expect(new AuthenticateResponse()).toBeTruthy();
  });
});
