import { ComSprinklerResponse } from './confirm-sprinkler-response';

describe('ComSprinklerResponse', () => {
  it('should create an instance', () => {
    expect(new ComSprinklerResponse()).toBeTruthy();
  });
});
