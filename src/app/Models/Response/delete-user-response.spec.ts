import { DeleteUserResponse } from './delete-user-response';

describe('DeleteUserResponse', () => {
  it('should create an instance', () => {
    expect(new DeleteUserResponse()).toBeTruthy();
  });
});
