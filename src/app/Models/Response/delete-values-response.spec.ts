import { DeleteValuesResponse } from './delete-values-response';

describe('DeleteValuesResponse', () => {
  it('should create an instance', () => {
    expect(new DeleteValuesResponse()).toBeTruthy();
  });
});
