import { DeleteLocationsResponse } from './delete-locations-response';

describe('DeleteLocationsResponse', () => {
  it('should create an instance', () => {
    expect(new DeleteLocationsResponse()).toBeTruthy();
  });
});
