import { OperationsResponse } from './operations-response';

describe('OperationsResponse', () => {
  it('should create an instance', () => {
    expect(new OperationsResponse()).toBeTruthy();
  });
});
