import { GetKeyWordsResponse } from './get-key-words-response';

describe('GetKeyWordsResponse', () => {
  it('should create an instance', () => {
    expect(new GetKeyWordsResponse()).toBeTruthy();
  });
});
