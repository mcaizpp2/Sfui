import { SaveConversionMappingsResponse } from './save-conversion-mappings-response';

describe('SaveConversionMappingsResponse', () => {
  it('should create an instance', () => {
    expect(new SaveConversionMappingsResponse()).toBeTruthy();
  });
});
