import { DeleteSprinklerResponse } from './delete-sprinkler-response';

describe('DeleteSprinklerResponse', () => {
  it('should create an instance', () => {
    expect(new DeleteSprinklerResponse()).toBeTruthy();
  });
});
