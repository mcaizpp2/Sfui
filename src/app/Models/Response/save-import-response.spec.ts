import { SaveConversionResponse  } from './save-import-response';

describe('SaveConversionResponse', () => {
  it('should create an instance', () => {
    expect(new SaveConversionResponse()).toBeTruthy();
  });
});
