import { UpdateValueResponse } from './update-value-response';

describe('UpdateValueResponse', () => {
  it('should create an instance', () => {
    expect(new UpdateValueResponse()).toBeTruthy();
  });
});
