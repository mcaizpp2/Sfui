import { ImportLookupResponse } from './import-lookup-response';

describe('ImportLookupResponse', () => {
  it('should create an instance', () => {
    expect(new ImportLookupResponse()).toBeTruthy();
  });
});
