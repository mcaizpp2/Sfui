import { DeleteLocationsRequest } from './delete-locations-request';

describe('DeleteLocationsRequest', () => {
  it('should create an instance', () => {
    expect(new DeleteLocationsRequest()).toBeTruthy();
  });
});
