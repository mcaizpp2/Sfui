import { DeleteValuesRequest } from './delete-values-request';

describe('DeleteValuesRequest', () => {
  it('should create an instance', () => {
    expect(new DeleteValuesRequest()).toBeTruthy();
  });
});
