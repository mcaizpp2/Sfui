import { GetWorkSheetsRequest } from './files-request';

describe('GetWorkSheetsRequest', () => {
  it('should create an instance', () => {
    expect(new GetWorkSheetsRequest()).toBeTruthy();
  });
});
