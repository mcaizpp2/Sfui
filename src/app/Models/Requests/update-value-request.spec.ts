import { UpdateValueRequest } from './update-value-request';

describe('UpdateValueRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateValueRequest()).toBeTruthy();
  });
});
