import { CreateEncoderRequest } from './create-encoder-request';

describe('CreateEncoderRequest', () => {
  it('should create an instance', () => {
    expect(new CreateEncoderRequest()).toBeTruthy();
  });
});
