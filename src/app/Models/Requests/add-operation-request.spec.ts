import { AddOperationRequest } from './add-operation-request';

describe('AddOperationRequest', () => {
  it('should create an instance', () => {
    expect(new AddOperationRequest()).toBeTruthy();
  });
});
