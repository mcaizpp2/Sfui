import { AuthenticateRequest } from './authenticate-request';

describe('AuthenticateRequest', () => {
  it('should create an instance', () => {
    expect(new AuthenticateRequest()).toBeTruthy();
  });
});
