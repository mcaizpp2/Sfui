import { CommitValuesRequest } from './commit-values-request';

describe('CommitValuesRequest', () => {
  it('should create an instance', () => {
    expect(new CommitValuesRequest()).toBeTruthy();
  });
});
