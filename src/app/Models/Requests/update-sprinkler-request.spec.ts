import { UpdateSprinklerRequest } from './update-sprinkler-request';

describe('UpdateSprinklerRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateSprinklerRequest()).toBeTruthy();
  });
});
