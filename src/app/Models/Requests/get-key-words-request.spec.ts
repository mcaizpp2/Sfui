import { GetKeyWordsRequest } from './get-key-words-request';

describe('GetKeyWordsRequest', () => {
  it('should create an instance', () => {
    expect(new GetKeyWordsRequest()).toBeTruthy();
  });
});
