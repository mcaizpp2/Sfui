import { LocUpdatesRequest } from './loc-updates-request';

describe('LocUpdatesRequest', () => {
  it('should create an instance', () => {
    expect(new LocUpdatesRequest()).toBeTruthy();
  });
});
