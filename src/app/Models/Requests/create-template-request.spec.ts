import { CreateTemplateRequest } from './create-template-request';

describe('CreateTemplateRequest', () => {
  it('should create an instance', () => {
    expect(new CreateTemplateRequest()).toBeTruthy();
  });
});
