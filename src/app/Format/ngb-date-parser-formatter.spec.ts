import { NgbDateFormatter } from './ngb-date-parser-formatter';

describe('NgbDateFormatter', () => {
  it('should create an instance', () => {
    expect(new NgbDateFormatter()).toBeTruthy();
  });
});
