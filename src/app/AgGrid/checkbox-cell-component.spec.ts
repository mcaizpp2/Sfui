import { CheckBoxCellComponent } from './checkbox-cell-component';

describe('CheckboxCellComponent', () => {
  it('should create an instance', () => {
    expect(new CheckBoxCellComponent(null)).toBeTruthy();
  });
});
