import { AdminValuesService } from './admin-values-service';

describe('AdminValuesService', () => {
  it('should create an instance', () => {
    expect(new AdminValuesService(null, null, null)).toBeTruthy();
  });
});
