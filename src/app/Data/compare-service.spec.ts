import { CompareService } from './compare-service';

describe('CompareService', () => {
  it('should create an instance', () => {
    expect(new CompareService(null, null, null)).toBeTruthy();
  });
});
