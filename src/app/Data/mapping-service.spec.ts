import { MappingService } from './mapping-service';

describe('MappingService', () => {
  it('should create an instance', () => {
    expect(new MappingService(null,null,null)).toBeTruthy();
  });
});
