import { EncorderService } from './encorder-service';

describe('EncorderService', () => {
  it('should create an instance', () => {
    expect(new EncorderService(null,null,null)).toBeTruthy();
  });
});
