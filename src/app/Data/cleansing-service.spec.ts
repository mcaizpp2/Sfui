import { CleansingService } from './cleansing-service';

describe('CleansingService', () => {
  it('should create an instance', () => {
    expect(new CleansingService(null, null, null)).toBeTruthy();
  });
});
